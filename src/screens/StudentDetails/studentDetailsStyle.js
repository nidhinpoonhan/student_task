const studentDetailsStyle = (theme) => ({
    root: {
        width: '90%',
        minHeight: '90%',
        margin: '12px auto',
        padding: 16,
    },
    barContainer: {
        maxWidth: 800,
    }
});

export default studentDetailsStyle;
